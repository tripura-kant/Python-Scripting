cluster = "eu01,eu02,eu03,eu04"  # Example cluster string

# Split the cluster string into a list using a comma as the delimiter
myList = cluster.split(',')

# Print the elements of myList
for element in myList:
    print(element)
